# QA

### Sau khi tôi dung lệnh `/init -p` -> nó tạo ra project voi #8. tuy nhiên khi run lệnh: `/import -f tasks_data.csv` thì nó lại import vào dự án #7
### Thêm thư viện colorama vào code để hiển thị màu giống như trên github